# Csv Webapp - Upload a CSV file and view data

This webapp allows users to store their CSV file and have the contents displayed in a web UI.

## To build and run
```
docker-compose build
docker-compose up
```

