version: '3'
services:
        backend:
                image: csv-demo/backend
                container_name: backend
                build: ./backend
                expose: 
                        - 8000
                volumes:
                        -static:/var/www/static

        frontend:
                image: csv-demo/frontend
                container_name: frontend
                build: ./frontend
        nginx:
                image: csv-demo/nginx
                container_name: nginx
                build: ./nginx
                links:
                        - backend
                        - frontend
                ports:
                        - "80:80"
                volumes:
                        -static:/static
volumes:
        static: 
